[package]
name = "effinterp-repo"
version = "0.1.0"
edition = "2024"

[dependencies]
effinterp-engine = { path = "../effinterp-engine" }
effinterp-proto = { path = "../effinterp-proto" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
